export type Persona = {
    dni: string;
    nombre: string;
    email: string;
    CodigoPostal: number;
    ISO: string;
    id: string;
  };
  export type Place = {
    city: string;
    country: string;

}

export type Temperatura = {
  temp_c: number;
}
